@model List<GetCategoryVM>


<h5 class="card-title fw-semibold mb-4">Category</h5>
<a asp-controller="Category" asp-action="Create" class="btn btn-success ">Create</a>
<div class="table-responsive" w-100>
    <table class="table table-bordered text-nowrap mb-0 align-middle">
        <thead class="text-dark fs-4">
            <tr>
                <th class="border-bottom-0">
                    <h6 class="fw-semibold mb-0">Id</h6>
                </th>
                <th class="border-bottom-0">
                    <h6 class="fw-semibold mb-0">Image</h6>
                </th>

                <th class="border-bottom-0">
                    <h6 class="fw-semibold mb-0">Sil/Duzelt</h6>
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var datas in Model)
            {
                <tr>
                    <td class="border-bottom-0">
                        @datas.Id
                    </td>
                    <td class="border-bottom-0">
                        @datas.Name
                    <td />

                    <a asp-action="Update" asp-route-id="@datas.Id" class=" btn btn-warning">Edit</a>
                    <a asp-action="Delete" asp-route-id="@datas.Id" class=" btn btn-warning">Delete</a>


                </tr>
            }
        </tbody>
    </table>
</div>